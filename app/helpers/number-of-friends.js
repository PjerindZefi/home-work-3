import { helper } from '@ember/component/helper';

export default helper(function numberOfFriends(params) {
    //printing out expenses
    //printing out friends
    return params[0] === params[1];
});